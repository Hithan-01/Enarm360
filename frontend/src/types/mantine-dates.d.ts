declare module '@mantine/dates' {
  // Minimal declarations to satisfy TypeScript in dev if local type resolution fails
  // You can replace `any` with specific types if needed
  export const DateInput: any;
}
